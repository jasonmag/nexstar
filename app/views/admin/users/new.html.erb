<% content_for :title, @user.new_record? ? "New User" : "Edit User" %>

<h1 class="text-2xl font-bold mb-6"><%= @user.new_record? ? "New User" : "Edit User" %></h1>

<%= render "form", user: @user %>

<div class="mt-6">
  <%= link_to "Back", admin_users_path, class: "text-indigo-600 hover:underline" %>
</div>
