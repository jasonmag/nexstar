<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Nexstar" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white text-gray-900 antialiased">
    <header class="bg-white shadow-md sticky top-0 z-30">
      <div class="max-w-7xl mx-auto px-4 py-6 flex justify-between items-center">
        <%= link_to "JasonMag", root_path, class: "text-2xl font-bold" %>
        <nav class="space-x-4 text-gray-700">
          <%= link_to "Articles", articles_path, class: "hover:text-indigo-500" %>
          <%= link_to "Projects", projects_path, class: "hover:text-indigo-500" %>
          <%= link_to "About", about_path, class: "hover:text-indigo-500" %>
        </nav>
      </div>
    </header>

    <main>
      <%= yield %>
    </main>

    <footer class="bg-gray-50 mt-16 py-8">
      <div class="max-w-4xl mx-auto text-center space-y-2 text-gray-600">
        <%= link_to "Contact", contact_path, class: "hover:text-indigo-500" %> ·
        <%= link_to "Articles", articles_path, class: "hover:text-indigo-500" %> ·
        <%= link_to "Projects", projects_path, class: "hover:text-indigo-500" %>
        <p class="mt-2 text-xs">&copy; <%= Time.current.year %> JasonMag</p>
      </div>
    </footer>
  </body>
  </html>
